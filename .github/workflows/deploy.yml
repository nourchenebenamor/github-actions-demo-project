name: deploy Project 

on:
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - uses: checkout@v3
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build 
